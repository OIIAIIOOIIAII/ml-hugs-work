# HUGS 实验进展汇报（2026/06/08 — 2026/06/09）

---

## 一、概述

本阶段围绕 **VIMO 粗对齐下 HUGS 崩坏问题的根因诊断与修复** 展开，经历了 **11 个会话（#33 — #43）**，核心工作包括：

1. **多种几何启发式方法的快速验证**（全部失败）
2. **STM FusionMLP 的移植与 Bug 修复**（v1→v4）
3. **六场景 Opacity Reset 对比实验**（彻底推翻往返假说，确定真正根因）
4. **Depth Hinge 约束的提出、修复与四场景泛化验证**
5. **Opacity Reset 根因的三组诊断实验**（隔离确认）
6. **Mask-Aware Prune 机制的实现与测试**

---

## 二、V2 完整验证结果（6场景 × 3方法）

> 截至 6/8 会话 #31，全部 6 场景的完整训练已完成。

| 场景 | OUR (HUMAN PSNR) | STM | HUGS Baseline | OUR − STM |
|------|--:|--:|--:|--:|
| **lab*** | **19.5512** | 17.4512 | 16.7751 | **+2.10** ✓ |
| **bike** | 18.0921 | **18.6133** | 17.0399 | -0.52 ✗ |
| **citron** | **17.4008** | 16.8763 | 15.5797 | **+0.52** ✓ |
| **jogging** | 16.9871 | **17.3776** | 16.1335 | -0.39 ✗ |
| **parkinglot** | **15.1924** | 14.3660 | 13.9266 | **+0.83** ✓ |
| **seattle** | 15.3117 | **16.7278** | 15.3684 | -1.42 ✗ |
| **avg (5场景, 无lab)** | 16.5968 | 16.7922 | 15.6096 | -0.20 |
| **avg (全6场景)** | 17.0892 | 16.9020 | 15.8039 | +0.19 |

> *lab 使用旧 ADC pipeline，其余 5 场景使用 inline_attn_18k pipeline + VIMO 对齐。

**结论**：OUR 在 3/6 场景超越 STM，但在往返轨迹场景（seattle/bike/jogging）表现较差，seattle 差距最大（-1.42 dB）。

---

## 三、6/8 实验：几何启发式方法 & FusionMLP 移植

### 3.1 几何启发式快速验证（全部失败）

在 seattle_vimo 场景上进行 3k 步快速验证，尝试了 6 种方案：

| 实验名 | 方案描述 | step 1k | step 3k | 结论 |
|---|---|--:|--:|---|
| exclusion30 | human_exclusion_w=30（几何排斥） | 15.70 | 14.92 | ❌ 最终崩坏 |
| opacity_suppress | scene GS opacity 压制（w=2.0） | 14.95 | 14.67 | ❌ 无效 |
| maskaware | scene GS 只从非人体像素学习 | 14.74 | — (kill) | ❌ 无效 |
| scene_delay2k | scene GS 延迟到 step 2000 开始优化 | 7.42 | — (kill) | ❌ 崩溃 |
| attn_warmup_opacity | AnchorAttn module_start=0 + correct_opacity | — | — (kill) | ❌ 未出结果 |
| attn_step0 (3k) | AnchorAttention 提前到 step 0 | 15.56 | 14.49 | ❌ 略好但不够 |

**核心结论**：所有几何启发式方法均失败。根因是 VIMO 误差导致 SMPL mesh 位置不准，几何判定不精确。

### 3.2 FusionMLP 移植（v1 → v4）

从 STM 的 `HumanSceneFuseDecoder`（ResidualMLP，fc2 zero init）移植到 HUGS：

| 版本 | 关键改动 | 3k HUMAN PSNR | 问题 |
|---|---|--:|---|
| **v1** | human+scene 全量，含 human xyz 修改 | 8.85 (step 1k) | ❌ human xyz 被破坏 |
| **v2** | 修正 property_dims（对齐 STM） | 12.74 (step 2k) | ❌ human xyz 仍被破坏 |
| **v3** | FusionMLP only scene，human 用原始输出 | — (kill) | renderer bug + validate bug |
| **v4** | 修复 renderer（全量 fused_gs_out）+ validate 传入 fused | **15.97** | ✅ v3→v4 **+3.97 dB** |

**v4 详细结果**（seattle_vimo, 3k 步）：

| Step | HUMAN PSNR |
|--:|--:|
| 1k | 14.58 |
| 2k | 15.53 |
| 3k | **15.97** |

**关键发现**：STM FusionMLP 无法直接移植的根本原因——STM 的 human GS 是 SMPL mesh 顶点（位置规则），而 HUGS 的 human GS 是 triplane 生成的 ~110K 高斯点，FusionMLP 修改动态生成的 human xyz 会破坏 SMPL 对齐。

### 3.3 STM 参数纠正

| 配置项 | OUR 基线 | STM 实际值 | 此前错误认知 |
|---|---|---|---|
| `depth_w` | 0.0 | **0.03** | 0 |
| `lbs_w` | 1000 | **100** | — |
| `scene.densify_from_iter` | 500 | **500**（默认值） | 曾误以为 3000 |

---

## 四、6/9 实验：Opacity Reset 根因确认

### 4.1 六场景 1k 探针实验（核心数据表）

val_interval=100，重点观察 step 400→500（opacity reset）→600（恢复能力）：

| Step | OUR seattle | OUR lab_vimo | STM seattle | OUR citron | OUR lab(GT) | OUR bike |
|------|--:|--:|--:|--:|--:|--:|
| 300 | 15.40 | 15.64 | 15.32 | 16.87 | 17.25 | 14.32 |
| **400** | **15.72** | **16.79** | **15.34** | **16.79** | **18.08** | **15.55** |
| **500 (reset)** | **8.86↓** | **9.25↓** | **7.47↓** | **8.78↓** | **9.19↓** | **12.07↓** |
| 600 | 14.13 | 15.91 | 7.68 | **16.74** | **17.43** | 14.20 |
| 700 | 15.11 | 16.48 | 15.43 | 16.72 | 18.30 | 15.07 |
| 1000 | 15.22 | **17.17** | 15.82 | 16.93 | **18.01** | 15.15 |

### 4.2 往返假说彻底推翻

| 场景 | 往返比 | step 500 崩坏 | step 600 恢复 | 结论 |
|---|--:|--:|--:|---|
| citron（果园） | **1.29** | 8.78 | **16.74（完全恢复）** | 100步恢复 |
| seattle（城市） | **1.32** | 8.86 | 14.13（未恢复） | 500步后仍低于 reset 前 |

两者往返比几乎相同，崩坏幅度相同，但恢复天壤之别。**往返不是根因。**

### 4.3 真正根因确认

> **复杂背景场景中，opacity reset 后 scene GS 在人体遮挡区域因强梯度信号被吸引进入人体区域，与 human GS 竞争，导致恢复缓慢。**

佐证数据：
- **lab**（室内简单背景，往返比=11.64）→ **完全恢复**
- **jogging**（室外复杂，往返比=15.44）→ **不恢复**
- **bike** step 500 崩坏最小（12.07），但恢复同样差 → 不是"进入多少 GS"的问题，是 densify 阶段梯度复杂度

### 4.4 三组诊断实验（隔离 opacity reset）

| 实验 | 配置变化 | step 500 PSNR | step 1000 PSNR | 结论 |
|---|---|--:|--:|---|
| diag_scene_no_densify | densify_from_iter=9999（禁用全部） | 平滑上升 ~16.0 | **16.31** | 对照：无操作最好 |
| diag_opacity_reset_only | 只有 opacity reset，无 densify | **8.93（崩坏）** | 16.41 | opacity reset 单独导致崩坏 |
| diag_densify_from_700 | 崩坏时机后移到 step 700 | 700步 8.73崩坏 | 13.80 | 时机跟随 → 确认根因 |

**结论：opacity reset（而非致密化）是 step 500 崩坏的直接根因。**

---

## 五、6/9 实验：Depth Hinge 约束

### 5.1 机制设计

- **约束**：惩罚 scene GS 渲染深度小于 human GS 渲染深度的情况（scene 不应出现在 human 前方）
- **Loss**：`l_scene_behind_human_depth = max(0, human_depth - scene_depth + margin)²`
- **关键修复**：`human_depth.detach()`（阻止梯度流向 human GS，只修正 scene GS）

### 5.2 四场景泛化实验结果

| 场景 | 初始 violation | 1k Baseline | 1k depth_hinge | 提升 | 结论 |
|---|--:|--:|--:|--:|---|
| **lab (GT)** | — | 18.01 | **18.59** | **+0.58** ✓ | 不崩坏场景有效 |
| **seattle** | 19.56 → 4.93 | **15.22** | 14.78 | **-0.44** ✗ | 崩坏场景无效甚至负面 |
| **bike** | 6.75 | 15.15 | 15.21 | +0.06 | 几乎无效 |
| **jogging** | 2.97 | 15.81 | 15.82 | +0.01 | 完全无效 |

**Seattle 详细步进对比**：

| Step | Baseline | Fixed depth_hinge |
|--:|--:|--:|
| 400 | 15.72 | 14.52 |
| **500 (崩坏)** | **8.86** | **9.42** |
| 600 | 14.13 | 14.96 |
| 1000 | **15.22** | **14.78** |

**核心结论**：
- depth_hinge 对 **不崩坏场景**（lab）有稳定 +0.58 dB 提升
- 对 **崩坏场景** 无效甚至有轻微负面效果
- 崩坏根因不在 scene GS depth 入侵

### 5.3 depth_hinge 失效机制分析

`depth_hinge` 依赖 `scene_depth`（scene GS alpha 加权渲染）。opacity reset 后 alpha→0 → scene_depth≈0 → depth_hinge loss≈0 → **梯度信号消失**。恰好在最危险的步骤（500-1000）depth_hinge 是无效的。

更深根因：scene GS 致密化用的是 `xyz_gradient_accum`（viewspace 梯度累积），不是 loss 梯度。depth_hinge 的 loss 梯度无法阻止 densification 决策。

---

## 六、6/9 实验：INVASION_COUNT 定量实验

### 6.1 实验设计

用 AABB 方法统计 scene GS 落入 human GS 包围盒内的数量（margin=0.1），每 100 步 validate，测 1000 步，4 场景并行。

### 6.2 完整数据

| Step | seattle（崩坏） | bike（崩坏） | citron（正常） | lab_vimo（正常） |
|--:|--:|--:|--:|--:|
| 100 | 391.6 | 541.6 | 457.9 | 566.3 |
| 500 | 565.7 | 577.8 | 627.4 | 553.9 |
| 700 | 751.1 | 660.3 | 772.3 | 720.0 |
| **1000** | **2599.0** | **2010.6** | **2647.3** | **2790.0** |

**结论：假说不成立。** 四条曲线几乎重叠，正常场景 lab_vimo=2790 反而最高，崩坏场景 bike=2010 最低。INVASION_COUNT 绝对值无法区分崩坏/不崩坏场景。

---

## 七、6/9 实验：Mask-Aware Prune

### 7.1 实现方案

**代码位置**：`hugs/trainer/gs_trainer.py` 第1748-1763行

每次 `densify_and_prune` 后立即执行：
1. 将 scene GS 中心投影到当前帧画面
2. 找出投影落在 human mask 内的 scene GS
3. 直接删除（`prune_points`）

与 `mask_aware_densify` 合用：densify 不创建新的 → prune 删除已有的 → human mask 内无 scene GS。

### 7.2 已有但被禁用的三个机制

| 机制 | 作用 | 局限 |
|------|------|------|
| `mask_aware_densify` | 排除 human mask 内 scene GS 的梯度累积 | 不阻止已有 GS 的 opacity 增长 |
| `mask_aware_loss` | scene 只在背景像素计算 L1 loss | 主联合 loss 仍给 human 像素梯度 |
| `human_region_opacity_suppress_loss` | 惩罚 human 包围球内 scene GS opacity | soft 约束，与主 loss 博弈 |

### 7.3 测试实验

- **配置**：bike 场景，1k 步，`mask_aware_enabled=true` + `mask_aware_densify=true` + `mask_aware_prune=true`
- **对照**：bike + vimo, 1k, 无 mask_aware → step 500 崩坏至 ~9.65, step 1000 = 15.15
- **状态**：PID 481840，正在运行（截至 6/9 会话 #43 结束时）

---

## 八、关键结论与认知迭代总结

### 8.1 根因认知演进

| 阶段 | 假说 | 验证结果 |
|---|---|---|
| 6/8 初 | 几何启发式可修复 | ❌ 6 种方法全部失败 |
| 6/8 中 | STM FusionMLP 移植可解决 | ⚠️ v4 修复后可用（15.97），但无法超越 STM |
| 6/8 末 | 往返轨迹是根因 | ❌ 6/9 六场景实验推翻（citron 同样往返但完全恢复） |
| 6/9 上 | scene GS densify_from_iter 差异 | ❌ STM 实际也是 500，非 3000 |
| 6/9 中 | opacity reset 是崩坏直接触发器 | ✅ 三组诊断实验确认 |
| 6/9 中 | 崩坏场景有更多 scene GS 入侵 | ❌ INVASION_COUNT 实验推翻 |
| 6/9 下 | depth hinge 可阻止 scene GS 入侵 | ❌ 对崩坏场景无效（梯度信号在 reset 后消失） |
| **6/9 最终** | **复杂背景梯度阻碍 reset 后恢复** | ✅ 六场景对比 + 诊断实验共同支持 |

### 8.2 当前最佳理解

> **Opacity reset 后所有场景都崩坏（6-9 dB），但简单背景场景（lab/citron）100 步内完全恢复，复杂背景场景（seattle/bike/jogging）恢复受阻。根因是复杂背景在人体遮挡区域产生强 viewspace 梯度 → scene GS densification 集中在 human mask 内 → 与 human GS 竞争 → 恢复缓慢。loss 梯度层面的约束（depth_hinge、opacity_suppress）无法阻止 densification 决策（基于 viewspace 梯度累积）。**

### 8.3 待验证方向

1. **mask_aware_prune**（已实现，待出结果）：硬性删除 human mask 内的 scene GS
2. **AnchorAttention start_iter=0**（1k 实验已跑完，效果有限：+0.22 dB）
3. **完整 18k depth_hinge 实验**（仅对不崩坏场景有价值）
