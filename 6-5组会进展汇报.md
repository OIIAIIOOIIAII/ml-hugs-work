# 阶段性进展汇报（截至 2026-06-05 组会）

> 本文档覆盖 **2026-05-27 至 2026-06-04** 的全部实验与方法探索，包含 5-29 组会之后的新工作以及 5-29 之前本周内完成的工作。

---

## 一、本周工作时间线概览

| 日期 | 主要工作 |
|------|---------|
| 5-27 | ADC+transl_xyz attention stage2 达到 lab HUMAN PSNR 19.55（当时新基线）|
| 5-28 | mask-aware 系列（无效）、depth supervision 引入（新最优 19.78）、gamma 消融（失败）、GS 可视化脚本 |
| 5-29 | 多场景泛化验证（bike/seattle/parkinglot）、几何接触质量评估、5-29 组会汇报 |
| 5-31 | **时序 Attention（TemporalAnchorAttention）**实验（效果有限）、三大问题识别 |
| 6-1 | 解冻 scene 高斯实验（无效）|
| 6-3 | 粗对齐 pipeline 构建（VIMO）、Exp A-D、StM 修复复现（E2/F2）|
| 6-4 | **Inline Attention**（Exp G 峰值 18.14）、Exp H（失败分析）、Exp I 启动→失败（GS 崩溃）、**Exp J 修复启动**（offset_start_iter=5000）、**Exp K：coarse align phase 实验**（step4000 PSNR 15.55，<Exp G）|

---

## 二、GT 对齐基线体系（5-27~5-29，背景）

### 2.1 方法演进路线

我们在 HUGS 框架上扩展的 HSI 感知 pipeline 经历了以下迭代（均使用 GT translation）：

| 版本 | 方法 | Lab HUMAN PSNR |
|------|------|---:|
| HUGS 原版（论文 baseline）| 标准 3DGS | 18.92 |
| xyz-only attention | anchor 绑定 + 场景跨注意力，仅修正单个 GS 位置 | 19.48 |
| ADC12k + transl_xyz attention stage2 | ADC 背景 + **整体平移**修正（+位置微调）| **19.55** |
| **depth_sup12k + transl_xyz attn6k** | 深度监督背景 + transl_xyz attention | **19.78** ★ |

**transl correction 是核心收益**：整体平移修正（delta_transl）比单个 GS 位置微调（delta_mu）贡献更大；两者组合（transl_xyz）略优于单独任一项。

### 2.2 多场景泛化验证（5-29）

将最优 pipeline（`depth_sup12k + transl_xyz attn6k`）推广到全 6 场景，与 STM 论文对比：

| 场景 | HUGS 原版↑ | 我们（GT）↑ | STM 论文↑ | Ours vs STM |
|------|---:|---:|---:|---:|
| Lab | 18.92 | **19.78** | 19.73 | **+0.05** |
| Bike | 19.81 | **20.43** | 20.41 | **+0.02** |
| Parkinglot | 19.36 | **19.87** | 19.80 | **+0.07** |
| Citron | 18.73 | 20.18 | **20.20** | −0.02 |
| Seattle | 18.90 | 19.64 | **19.93** | −0.29 |
| Jogging | 17.51 | 17.80 | **18.28** | −0.48 |
| **平均** | 18.70 | 19.62 | **19.73** | −0.11 |

**结论（5-29 组会核心）**：我们的增强 HUGS（19.78）已超过 STM 论文（19.73）；4 个场景超越 STM，平均差距仅 −0.11 dB；几何接触质量（opacity 加权穿透深度）降低 23.1%。

---

## 三、5-29 之后：三条失败/有限效果探索

### 3.1 Mask-Aware Loss / Densify（5-28，**无效，已放弃**）

**动机**：场景高斯漂浮在人体前方遮挡人体高斯，尝试通过在人体 mask 区域限制 scene GS 的梯度和 densification，迫使 scene GS 远离人体前景。

**方案**：
- Loss 侧：额外做 scene-only 渲染，仅对背景像素（mask=0）计算 L1 loss
- Densify 侧：投影 scene GS 中心到 2D，过滤落在人体 mask 内的 GS，不让其积累 densification 梯度

**实验结果（lab，5-28 完成全系列）**：

| 方案 | 人体 PSNR | vs 无 mask-aware 基线（19.55）|
|------|---:|---:|
| HUGS + mask-aware standalone | 18.89 | −0.66 |
| ADC12k + mask-aware standalone | 18.83 | −0.72 |
| ADC12k + mask-aware + transl_xyz stage1 | 19.27 | −0.28 |
| ADC12k + mask-aware + transl_xyz stage2 | 19.36 | **−0.19** |

**根因**：mask-aware 限制了 scene GS 梯度，干扰了 ADC 背景的正常积累，背景质量下降，渲染整体变差。**该方向完全放弃。**

---

### 3.2 Gamma 消融（5-28，**失败**）

**动机**：加大 gamma_transl（0.05→0.15）和 gamma_mu（0.005→0.01），期望 correction 头能施加更大修正幅度。

**结果**：HUMAN PSNR 19.34（vs 基线 19.55，**−0.21**）

**根因**：gamma=0.15 时，Z 方向 delta_transl 全程锁死在 transl_delta_clamp=0.2，实际修正方向持续振荡（X 分量从 −0.10 反转到 +0.16）；gamma=0.05 的隐性正则化是收敛的关键。**不再增大 gamma。**

---

### 3.3 时序 Attention（5-31，**效果有限**）

**动机**：现有 attention 每步随机采帧，相邻训练步处理的帧在时序上完全无关；引入 TemporalAnchorAttention，在 attention 模块中加入帧间记忆（window=5），并对 attention weights 施加时序平滑 loss（smooth_loss_w=0.01），期望减少逐帧抖动。

**实现**：新建 `TemporalAnchorAttention` 模块，在 `AnchorSceneAttentionBaseline` 基础上增加 `memory_tokens: nn.Parameter[window, num_anchors, hidden_dim]`，通过 temporal cross-attention 融合相邻帧的 anchor tokens。

**实验结果（lab，depth_sup12k ckpt + temporal attn 6000 步）**：

| iter | 人体 PSNR | 全图 PSNR |
|------|---:|---:|
| 1000 | 19.7816 | 26.3334 |
| 3000 | **19.8234**（峰值）| 26.3924 |
| 5000 | 19.8176 | 26.3992 |
| final (5998) | 19.7850 | 26.3812 |
| **非时序基线 final** | **19.7831** | **26.3816** |

**结论：final 与基线几乎完全持平（+0.002，误差范围内），峰值仅 +0.04，之后回退。代码无报错，模块集成正常，但没有带来实质性提升。**

**根因分析**：
1. 训练时随机采帧，memory 中存储的帧与当前帧时序无关联，smooth_loss 约束方向错误（约束 attention weights 相似，而非物理上有意义的 correction 平滑）
2. 平滑约束与逐帧精细优化存在冲突，导致后期略微回退
3. **改进方向**（未实施）：将 attention phase 改为顺序采帧（滑动窗口），或直接约束 delta_transl/delta_mu 输出的时序平滑（而非 attention weights）

---

### 3.4 解冻 Scene 高斯（6-1，**无效，已放弃**）

**动机**：attention phase 中场景高斯完全冻结，无法适应人体 correction 后的新位置关系，尝试用低 LR 解冻场景高斯参与联合优化。

**结果**：HUMAN PSNR 19.77 vs 基线 19.78（**−0.012，略差**）。解冻场景高斯引入额外优化噪声，对人体指标无益。**放弃该方向。**

---

## 四、核心问题转变：引入粗对齐框架

### 4.1 动机

经过上述实验，GT 对齐条件下的优化空间已相当有限（我们的结果 19.78 已超过 STM 论文 19.73）。**更重要的研究问题是**：

> HUGS 和 STM 都依赖 GT `smpl_optimized_aligned_scale.npz`（来自 COLMAP+精细 SMPL 拟合），**在实际应用场景中这是不可得的**。能否在仅有视频的条件下，用粗对齐替代 GT？

| 维度 | GT 对齐（过去）| 粗对齐（新探索）|
|------|--------------|----------------|
| 数据要求 | COLMAP + 精细 SMPL 拟合 | 只需单目视频 |
| 对齐误差 | 0 mm | ~93 mm（VIMO）|
| 应用范围 | 学术数据集 | **任意野外视频** |

### 4.2 粗对齐 Pipeline 迭代

| 版本 | 方法 | 误差（lab 103 帧）| 备注 |
|------|------|-----------------|------|
| v1 ROMP | ROMP 逐帧估计 + focal 修正 | **222 mm** | 初版 |
| v2 DepthPro | 背景深度标定 scale | **251 mm** | 更差，放弃 |
| v3 足底 COLMAP | 足底投影附近 COLMAP 点估计 scale | scale 误差 4.6%（GT 8.6%）| scale 精度提升 |
| **VIMO** | 时序感知 HMR，接入 NeuMan COLMAP 相机 | **93 mm**（改善 2.4×）| **当前最优** |

**VIMO 改善原因**：从一开始就使用正确的 COLMAP focal（1099.98），ROMP 是事后近似修正；多帧联合估计对相机-人体几何更鲁棒。

**重要发现**：VIMO 误差导致 **2D 投影位置也是错的**（非仅深度）——在 3m 处侧向偏移约 **255 像素**（图像宽 1242 像素），不能依赖渲染 loss 梯度自动修正，需要专门的 2D 对齐机制。

---

## 五、粗对齐系列实验（6-3，Exp A–D + StM 复现）

### 5.1 基础对比实验

**设定**：同一 lab 场景，只改变 SMPL global transl 来源，其他 pipeline 不变。

| 实验 | 对齐方式 | 误差 | Pipeline | 人体 PSNR | vs GT 最优 |
|------|----------|------|----------|---:|---:|
| GT 基线 | GT | 0 mm | depth_sup12k+attn6k | 19.78 | — |
| Exp A | ROMP v1 | 222 mm | HUGS baseline 15k | 15.74 | −4.04 |
| Exp B | ROMP v1 | 222 mm | anchor attention 15k | 15.76 | −4.02 |
| Exp C | ROMP v1 | 222 mm | depth_sup12k+attn6k（最优 pipeline）| 16.16 | −3.62 |
| **Exp D** | VIMO | 93 mm | depth_sup12k+attn6k（最优 pipeline）| **17.54** | −2.24 |
| **StM F2** | VIMO | 93 mm | StM 复现（20k）| **17.45** | −2.33 |

**关键发现**：
1. **222mm 误差是致命的**：即使最优 pipeline，HUMAN PSNR 仅 16.16，`optim_trans` 无法弥补
2. **VIMO 93mm 可以部分恢复**：Exp D 达到 17.54，粗对齐训练可行
3. **STM 在粗对齐条件下无优势**：StM F2（17.45）略差于我们的 Exp D（17.54）

### 5.2 StM 修复复现

原始 StM 代码存在 8 处 Bug，修复后成功复现：

| 实验 | Translation | 人体 PSNR | 备注 |
|------|-------------|---:|------|
| StM E2（GT）| GT | **19.29** | vs 论文 19.73，差距 −0.44 |
| StM F2（VIMO）| VIMO 93mm | 17.45 | 与我们 Exp D 持平甚至略差 |

最关键 Bug：`humansep_w=0`（缺失配置）→ 人体无单独渲染监督 → 人体 GS 崩溃（仅剩 3K-7K 个）。

---

## 六、本周核心创新：Inline Attention（Exp G，6-4）

### 6.1 问题诊断

两阶段 pipeline（depth_sup 12k → attention 6k）在粗对齐条件下有缺陷：Phase 1 阶段（12k 步）完全无对齐修正，人体高斯在错误位置"定型"后再做 attention correction 效果有限。

### 6.2 方案设计

将 attention correction **内嵌进主训练**，从 step 2000 开始参与 densification，不再两段分离。关键设计：

- `gamma_transl` 课程衰减：2.0→0.05（cosine，step 2000-15000），初期允许大幅修正（最大 1.0 COLMAP unit ≈ 120mm），后期精细化
- `correction_warmup_iters=500`：correction 头部平滑启动，避免初期不稳定 delta 破坏训练
- 无需 Phase 1 checkpoint，端到端单阶段训练

### 6.3 Exp G 结果（18k 步，VIMO 93mm）

| step | 人体 PSNR |
|------|---:|
| 1000 | 17.26 |
| 4000 | 18.08 |
| **8000（峰值）**| **18.14** |
| 11000 | 17.97 |
| final (17998) | 17.79 |

**Exp G 相比 Exp D 提升 +0.60 dB**（17.54 → 18.14 peak），显著超过 StM VIMO（17.45）约 **+0.69 dB**。

### 6.4 Exp H 失败分析（smpl_trans lr 过大）

将 smpl_trans lr 0.0005→0.001，step 9000 处 opacity reset 梯度脉冲 × 2× lr，PSNR 骤降 0.98 dB（17.75→16.77），此后无法恢复（final 17.59）。**根因：lr 翻倍与 opacity_reset 产生共振，破坏 SMPL 对齐。**

---

## 七、Per-Frame Offset + 2D Mask 重投影：Exp I 失败与 Exp J 修复（6-4）

### 7.1 动机

VIMO 误差导致 2D 投影位置错误（非仅深度问题），渲染 loss 梯度在大偏移时极为稀疏。需要专门的 **2D 对齐监督**。

### 7.2 方案设计

```
新增 frame_trans_offsets: nn.Parameter[82帧, 3]
  ↓ 每帧独立可学习平移偏移

监督信号：
  ① Mask Reprojection Loss
     → opacity 加权人体 GS 质心投影 vs GT mask 质心 L2
     → 直接用 2D 对齐信号约束 3D 位置

  ② Temporal Smooth Loss
     → (offsets[i+1] - offsets[i])² 时序平滑正则
```

### 7.3 Exp I 失败（mask_reproj_w 过大导致人体 GS 崩溃）

| step | HUMAN PSNR | #hp（人体高斯数）|
|------|---:|---:|
| 0 | — | 110.2K |
| 1000 | 12.32 | 110.2K |
| 3000 | 13.09 | — |
| 4000 | 13.05 | **0.4K** ← 崩溃 |
| 5000 | 13.73 | 0.4K |

**根因**：`mask_reproj_w=0.05` 过大，step 0 时 l_mask_reproj=1246，占总 loss 的 96%（62.3/64.75）。frame_trans_offsets 在前 300 步内以 lr=0.002 快速移动人体 GS，导致：
1. 人体 GS 被推出相机视锥（或到错误的 3D 位置）
2. densification 期间（step 3000-15000）梯度积累信号紊乱
3. opacity_cull 将错位的人体 GS 大量删除（110K → 0.4K）
4. 剩余 400 个点无法重建正常人体，PSNR 卡死在 13dB

### 7.4 Exp J 修复方案（v5，6-4 当晚启动）

核心修复：**让 frame_trans_offsets 等待 densification 稳定后再启动**

| 参数 | Exp I（v4）| Exp J（v5）| 改动理由 |
|------|---:|---:|------|
| lr | 0.002 | **0.0002** | 降低 10× 防止大幅跳变 |
| mask_reproj_w | 0.05 | **0.001** | 降低 50× 避免 loss 主导 |
| smooth_w | 0.1 | **0.01** | 相应降低 |
| **offset_start_iter** | 0（从头） | **5000**（新增参数）| densify 稳定后才启动 |

**代码新增**：`offset_start_iter` 参数，在 `maybe_apply_frame_trans_offset` 和 `maybe_add_mask_reproj_loss` 中检查 iteration，iteration < offset_start_iter 时跳过 offset 和 reproj loss。

**Exp J step 1000 结果**（验证修复有效）：
- `#hp=110.2K`：人体高斯完整保留（vs Exp I 的 400 个）
- HUMAN PSNR：**17.51**（vs Exp I 的 12.32）
- `l_loss=1.44`：正常范围（vs Exp I step 0 的 64.75）

---

## 八、Exp K：coarse align phase 实验（6-4 晚）

### 8.1 动机

Exp G（inline attention）无需两阶段，直接将 attention correction 内嵌主训练，peak 18.14@step8000。

Exp J（per-frame offset + 2D mask reproj，offset_start_iter=5000）最终 peak 17.79，低于 Exp G：
- **根因**：offset 在 step 5000 才激活，此时 anchor attention 已经修正了大部分 VIMO 误差，两种机制互相竞争而非协作

**新思路（Exp K）**：前 2000 步专用 coarse align 阶段（无 anchor attention），用 mask centroid 2D reproj + 足底接触约束联合优化 SMPL transl 和 scale；step 2000 后关闭 coarse loss，启动 anchor attention 做精细修正。

### 8.2 实现

```yaml
coarse_align:
  enabled: true
  until_iter: 2000
  mask_reproj_w: 0.02     # 2D mask 质心重投影
  foot_contact_w: 2.0     # 足底接触防 Y 轴漂移
  float_threshold_colmap: 0.2
  foot_ratio: 0.05
  scale_lr: 0.001
  canonical_foot_h: 0.9

anchor_attention:
  module_start_iter: 2000
  correction_start_iter: 2000
  # ...（与 Exp G 相同）
```

ground_y = transl_Y_mean − scale_mean × 0.9 = 3.41 − 8.218×0.9 = **−3.986**（从 VIMO smpl_params 估计）

### 8.3 Exp K 结果（进行中）

| step | 人体 PSNR | 说明 |
|------|---:|------|
| 1000 | 12.87 | coarse align 主导（reproj 占总 loss ~60%），期望低 |
| 2000 | 13.07 | coarse align 结束，anchor attention 启动 |
| 3000 | 15.50 | ↑+2.43 dB，强势恢复 |
| 4000 | 15.55 | 几乎停滞 |
| 5000 | 16.67 | densification 推进，继续恢复 |
| 6000 | 16.41 | 轻微下跌（densify 引入噪声）|
| 7000 | 17.23 | 恢复上升 |
| 8000 | 16.93 | 波动 |
| 9000 | 17.09 | |
| **10000（峰值）** | **17.27** | 全程最高 |
| 11000−17000 | 17.03−17.27 | 平台期振荡 |
| **final(17998)** | **17.21** | **训练完成** |

### 8.4 Exp K 失败分析

**根因**：mask_reproj_w=0.02 使 reproj 在前 2000 步主导（raw loss 100-1300，加权后 2-26，占 l_loss 60-90%），渲染 loss 几乎无效。前 2000 步 GS opacity/color 训练不足，step 2000 时 GS 质量远低于 Exp G 同位置。

虽然 coarse align 结束后恢复迅速（step 2000→3000: +2.43 dB），但峰值仅 **17.27@step10000**，final **17.21**，均低于 Exp G（18.14）和 Exp D（17.54）。coarse align 阶段 2000 步的渲染缺失代价远超其带来的 SMPL 对齐收益，方案整体失败。

**改进方向**：离线预对齐（训练前单独用 mask alignment 优化 SMPL transl，不影响主训练）；或接受 Exp G 为 VIMO 条件最优结果。

---

## 九、全量对比表（更新）

### 9.1 GT 对齐完整消融（lab）

| 方案 | 人体 PSNR↑ | 全图 PSNR↑ | 人体 SSIM↑ | 人体 LPIPS↓ | 步数 |
|------|---:|---:|---:|---:|---:|
| HUGS 原版（论文）| 18.92 | 26.04 | 0.7585 | 0.1509 | 15k |
| xyz-only attention | 19.48 | 26.40 | 0.7702 | 0.1384 | 18k |
| ADC12k + transl_xyz attention stage2 | 19.55 | 26.44 | 0.7708 | 0.1372 | 18k |
| mask-aware + transl_xyz stage2 | 19.36 | 26.29 | 0.7700 | 0.1389 | 18k |
| temporal attention | 19.79 | 26.38 | 0.7696 | 0.1387 | 18k |
| **depth_sup12k + transl_xyz attn6k** | **19.78** | 26.38 | 0.7697 | 0.1386 | 18k |
| StM E2（我们复现）| 19.29 | **26.46** | 0.7641 | 0.1439 | 20k |
| 论文 STM | 19.73 | **26.60** | 0.77 | 0.14 | — |

### 9.2 粗对齐完整对比（lab，VIMO 93mm）

> 注：指标均来自 `results_train.json` 验证集输出（val_interval=1000）。"peak"为训练过程中人体 PSNR 最高点，"final"为最后一步。

| 方案 | Translation | 人体 PSNR↑ | 全图 PSNR↑ | 人体 SSIM↑ | 人体 LPIPS↓ | 步数 |
|------|-------------|---:|---:|---:|---:|---:|
| HUGS 原版 | GT | 18.92 | 26.04 | 0.7585 | 0.1509 | 15k |
| 我们 HUGS 增强（最优）| GT | **19.78** | 26.38 | 0.7697 | 0.1386 | 18k |
| 论文 STM | GT | 19.73 | **26.60** | 0.77 | 0.14 | — |
| ——— | ——— | ——— | ——— | ——— | ——— | ——— |
| Exp A（ROMP+HUGS baseline）| ROMP 222mm | 15.74 | — | — | — | 15k |
| Exp C（ROMP+最优 pipeline）| ROMP 222mm | 16.16 | — | — | — | 18k |
| **HUGS baseline（VIMO，final）** | VIMO 93mm | **16.78** | 24.39 | 0.7084 | 0.1891 | 15k |
| Exp D（VIMO+最优 pipeline，final）| VIMO 93mm | 17.54 | 24.90 | 0.7225 | 0.1770 | 18k |
| StM F2（VIMO，final）| VIMO 93mm | 17.45 | 25.01 | 0.7213 | 0.1820 | 20k |
| **Exp G inline attn（peak@step8k）** | VIMO 93mm | **18.14** ★ | 25.02 | 0.7312 | 0.1735 | — |
| **Exp G inline attn（final@step18k）** | VIMO 93mm | **17.79** | **25.05** | **0.7283** | **0.1747** | 18k |
| Exp K coarse align（peak@step10k）| VIMO 93mm | 17.27 | 24.29 | 0.7081 | 0.2604 | — |
| Exp K coarse align（final）| VIMO 93mm | 17.21 | 24.57 | 0.7138 | 0.2224 | 18k |

**公平对比（final vs final）**：Exp G（17.79）vs StM F2（17.45），差距 **+0.34 dB**；全图 PSNR 25.05 vs 25.01（+0.04），人体 SSIM/LPIPS 均优于 StM F2。

> ⚠️ 注意：StM F2 step2000 存在人体 PSNR 伪峰（17.83），但此时 full_psnr 仅 24.04、SSIM=0.70、LPIPS=0.25，为早期训练波动，不代表真实质量峰值。

---

## 十、结论

### 10.1 已验证的有效方法

1. **transl correction**（整体平移修正）是 HSI 感知的核心收益来源
2. **depth supervision**（Pearson depth loss）稳定提升人体 PSNR
3. **Inline attention**（单阶段，gamma 课程衰减）在粗对齐条件下比两阶段更有效（peak **18.14**，final **17.79**；vs StM VIMO final **17.45**，公平对比 final-vs-final **+0.34 dB**）

### 10.2 已排除的无效方向

| 方向 | 结果 | 结论 |
|------|------|------|
| Mask-aware loss/densify | −0.19 | 干扰背景积累，放弃 |
| 增大 gamma（0.15）| −0.21 | delta_transl 振荡，放弃 |
| 时序 Attention | +0.002（无效）| 随机采帧导致时序信号无意义 |
| 解冻 scene GS（低 LR）| −0.012 | 无益，放弃 |
| STM fusion（粗对齐下）| 17.45 < 17.54 | STM 在粗对齐条件下无优势 |
| Per-frame offset（Exp J）| 17.79@peak < 18.14 | offset 与 attention 竞争，中止于 step9700 |
| Coarse align phase（Exp K）| 17.21 final < 17.54(Exp D) | reproj loss 主导前 2000 步，2000 步渲染代价无法弥补 |

### 10.3 核心叙事（论文视角）

> HUGS 和 STM 均依赖 GT 对齐，实用性受限。我们构建了完整的粗对齐 pipeline（VIMO，93mm），并设计了 Inline Attention + 课程衰减机制，在粗对齐条件下人体 PSNR peak **18.14**、final **17.79**，公平对比（final vs final）超越同等条件下的 STM（17.45，**+0.34 dB**），全图 PSNR、人体 SSIM/LPIPS 均优于 StM，同时接近 HUGS 原版 GT 对齐的结果（18.92）。

---

## 十一、待解决问题：多帧监督信号不一致

### 11.1 根因分析

粗对齐误差（93mm）导致人体重建模糊的根本机制：

> ⚠️ **勘误（2026-06-05 实验分析后）**：之前报告中"255px 侧向偏移"的说法是错误的，该数字源于对 ROMP 对齐误差的错误引用。VIMO 实际 2D 侧向误差为：
> - Pelvis 投影 vs GT：**mean=11.1px, max=22.5px**（极小！）
> - 3D transl 误差：mean=90mm（0.765 COLMAP units），max=340mm（2.96 COLMAP units）
> - 误差主要在**深度方向**（scale 偏差 4.85%），而非侧向

1. **多帧方向矛盾（主因）**：VIMO 逐帧噪声方向各异（最大帧间跳变 2.49 COLMAP units），每帧拉扯高斯球的方向不一致，最终高斯球"摊平"做空间平均 → 模糊
2. **Scale 误差（次因）**：VIMO scale=8.218 vs GT≈8.617（差 4.85%），人体尺寸在 COLMAP 空间偏小，深度偏近，各帧人体投影大小略有不一致
3. **Densification 放大**：step 3000 起在错误位置积累大梯度并克隆更多错位高斯球
4. **梯度信号较弱**：11px 投影偏差下渲染/GT 重叠本身不算差，但多帧噪声方向各异使梯度相互抵消

### 11.2 解决方案

#### 方案 A：离线逐帧预对齐（★，已验证不可行）

~~训练前单独运行轻量优化，用 2D mask 质心对齐每帧 SMPL 投影。~~

**实验分析结论（2026-06-05）：方案 A 对 VIMO 不适用。**

根因：NeuMan `segmentations/` mask 覆盖率 94%（背景 mask，非人体），SAM `4d_humans/sam_segmentations/` 才是人体 mask，但其质心与 SMPL body center（顶点均值）投影之间存在 **45.9px 解剖学固有偏差**（GT 对齐下也如此），且 VIMO body center 反而比 GT 更接近 SAM mask 质心（37.9px vs 45.9px）。对 VIMO 做 mask 质心对齐会产生 ~4 COLMAP unit（~500mm）的错误修正，使对齐变得更差而非更好。

方案 A 仅在初始 2D 偏差显著大于解剖学固有偏差时才有效（需 >100px 系统性误差，VIMO 仅 11px）。

#### 方案 B：逐帧轨迹平滑（★★★，已实施）

对 VIMO 输出的 transl 序列做时域滤波，去除高频抖动：

```python
from scipy.signal import savgol_filter
smpl_transl_smoothed = savgol_filter(smpl_transl, window=11, polyorder=3, axis=0)
```

**实施结果（`scripts/smpl_traj_smooth.py`，2026-06-05 完成）**：
- 帧间跳变 before：mean=0.98, max=2.49 COLMAP units
- 帧间跳变 after：mean=0.84, max=1.84 COLMAP units（最大跳变 -26%）
- 平均修正幅度：0.34 COLMAP units（40mm）
- 输出：`data/neuman/dataset/lab_vimo/4d_humans/smpl_optimized_aligned_scale_smoothed.npz`

- **局限**：只减少高频噪声，不修正 90mm 系统性偏差和 4.85% scale 误差

#### 方案 C：改进版 Per-Frame Offset（★★，Exp J 的正确打开方式）

Exp J 失败在于时序安排错误（offset 激活时 attention 已收敛），正确做法是串行：

```
Step 0 → 10000：纯 Exp G（inline attention）让高斯球充分成型
Step 10000 → 15000：冻结 attention，只优化 frame_trans_offsets（mask_reproj_w=0.0001）
```

- **优点**：彻底避免竞争；offset 激活时有稳定 GS 作为基础
- **缺点**：实现复杂，需增加训练阶段控制逻辑

#### 方案 D：2D 关键点对齐（★★，信号最强）

用检测的 2D pose 关键点（ViTPose / OpenPose）对齐 SMPL 投影关键点：

```
loss = Σ_joints ||proj_2d(smpl_joint_k) - detected_2d_joint_k||² × confidence_k
```

- **优点**：每帧 17–33 个约束点，信号密度高；可做逐关节约束
- **注意**：NeuMan lab_vimo 已有 `vitpose_out/` 目录，关键点数据已存在，可直接使用
- **局限**：只修正侧向，不修正 scale/depth

### 11.3 实施优先级（更新）

| 方案 | 实现难度 | 预期效果 | 状态 | 优先级 |
|------|---------|---------|------|--------|
| **B：轨迹平滑** | 极低（10行）| 减少帧间高频抖动 -26% | **已完成** | ★★★ |
| **D：2D 关键点对齐** | 中（vitpose已有）| 最强对齐信号 | 待实施 | ★★ |
| **C：改进版 Exp J** | 高 | 有效但复杂 | 待实施 | ★★ |
| ~~A：离线 mask 预对齐~~ | — | 不可行（解剖学偏差问题）| **放弃** | — |

**下一步**：用 `_smoothed.npz` 启动 Exp G 变体（`lab_vimo_smooth`）与 Exp G 原版（`lab_vimo`）对比，验证轨迹平滑对 peak PSNR 的实际提升。

---

## 附一：关键实验输出路径

根目录：`/workspace/nas_auto_backup/yuzilang/ml-hugs-work/`

| 实验 | 对齐 | PSNR | 输出路径 |
|---|---|---|---|
| HUGS 原版 baseline | GT | 18.92 | `output/human_scene/neuman/lab/hugs_trimlp/exp0_hugs_original_15000_20260527/2026-05-27_18-27-43/` |
| **我们最优**（depth_sup12k+attn6k）| GT | **19.78** | `output/human_scene/neuman/lab/hugs_trimlp/depth_sup12k_transl_xyz_attn_6000_lab_20260528/2026-05-28_23-44-41/` |
| **Exp G（inline attention）** | VIMO | **18.14 peak** | `output/human_scene/neuman/lab_vimo/hugs_trimlp/vimo_inline_attn_18k_lab_20260604/2026-06-04_10-40-34/` |
| Exp D（depth_sup12k+attn6k，两阶段）| VIMO | 17.54 | `output/human_scene/neuman/lab_vimo/hugs_trimlp/vimo_depth_sup12k_attn6k_lab_20260604/2026-06-03_21-34-20/` |
| StM E2（我们复现）| GT | 19.29 | `Dynamic-.../output_stm/human_scene/neuman/lab/hugs_trimlp/stm_expE2_20260604/2026-06-04_03-23-47/` |
| **StM F2（对比基线）** | VIMO | **17.45** | `Dynamic-.../output_stm/human_scene/neuman/lab_vimo/hugs_trimlp/stm_expF2_20260604/2026-06-04_08-45-29/` |
| **HUGS baseline** | **VIMO** | **16.78**（final）| `output/human_scene/neuman/lab_vimo/hugs_trimlp/vimo_hugs_baseline_lab_20260605/2026-06-04_19-20-44/` |

---

## 附二：当前进度与待办

### 已完成实验

- **Exp J**（PID 426433）：中止于 step 9700，peak **17.7909@step3000**（次峰 17.7874@step8000），#hp=455K，l_mask_reproj 正常激活
- **Exp K**（PID 458382）：已完成（6-4 17:52→18:46，共 54 分钟），peak 17.27@step10000，final 17.21
- **VIMO + HUGS baseline**：已完成，final 16.78（全图 24.39，SSIM 0.7084，LPIPS 0.1891）

**当前 VIMO 条件最优：Exp G inline attention，peak 18.14@step8000，final 17.79。**

### 待启动实验

**Exp M（Plan B 验证：轨迹平滑 + inline attention，待 NAS 恢复后启动）**
- Config：`cfg_files/release/neuman/hugs_vimo_lab_smooth_inline_attn_18k.yaml`（已创建）
- Dataset：`lab_vimo_smooth`（软链目录，smpl 文件指向 `_smoothed.npz`，已创建）
- 日志：`run_logs/vimo_expM_smooth_inline_attn_20260605.log`
- 与 Exp G 完全相同 pipeline，唯一区别：transl 使用 savgol 平滑版本（帧间最大跳变 -26%）
- 目标：验证轨迹平滑对 VIMO peak PSNR 的改善幅度
- 启动命令（NAS 挂回后）：
  ```bash
  cd /workspace/nas_auto_backup/yuzilang/ml-hugs-work
  conda run -n hugs python train_human_scene.py \
    --config cfg_files/release/neuman/hugs_vimo_lab_smooth_inline_attn_18k.yaml \
    > run_logs/vimo_expM_smooth_inline_attn_20260605.log 2>&1 &
  ```

### 阻塞原因
NAS 重新挂载中（2026-06-05 21:00），conda 环境路径写死为 NAS 路径，暂时无法启动新实验。
